@model EmployeeViewModel

@inject IUnitOfWork _unitOfWork;

@{
    var department = new SelectList(_unitOfWork.DepartmentRepository.GetAll(), nameof(Department.Id), nameof(Department.Name));


}

<div class="form-group">
    <label class="col-form-label" asp-for="Name"></label>
    <input asp-for="Name" class="form-control" />  @* asp-for="Name" ==> insted of { type="text" name="Code" id="code" } it will knows it from it self *@
    <span asp-validation-for="Name" class="text-danger"></span> @* Message of the error *@
</div>
<div class="form-group">
    <label class="col-form-label" asp-for="Age"></label>
    <input asp-for="Age" class="form-control" />  @* asp-for="Name" ==> insted of { type="text" name="Code" id="code" } it will knows it from it self *@
    <span asp-validation-for="Age" class="text-danger"></span> @* Message of the error *@
</div>
<div class="form-group">
    <label class="col-form-label" asp-for="Address"></label>
    <input asp-for="Address" class="form-control" />  @* asp-for="Name" ==> insted of { type="text" name="Code" id="code" } it will knows it from it self *@
    <span asp-validation-for="Address" class="text-danger"></span> @* Message of the error *@
</div>
<div class="form-group">
    <label class="col-form-label" asp-for="Salary"></label>
    <input asp-for="Salary" class="form-control" />  @* asp-for="Name" ==> insted of { type="text" name="Code" id="code" } it will knows it from it self *@
    <span asp-validation-for="Salary" class="text-danger"></span> @* Message of the error *@
</div>
<div class="form-check">
    <input asp-for="IsActive" class="form-check-input" />  @* asp-for="Name" ==> insted of { type="text" name="Code" id="code" } it will knows it from it self *@
    <label class="col-form-label" asp-for="IsActive"></label>
    @* Message of the error *@
</div>
<div class="form-group">
    <label class="col-form-label" asp-for="Email"></label>
    <input asp-for="Email" class="form-control" />  @* asp-for="Name" ==> insted of { type="text" name="Code" id="code" } it will knows it from it self *@
    <span asp-validation-for="Email" class="text-danger"></span> @* Message of the error *@
</div>
<div class="form-group">
    <label class="col-form-label" asp-for="PhoneNumber"></label>
    <input asp-for="PhoneNumber" class="form-control" />  @* asp-for="Name" ==> insted of { type="text" name="Code" id="code" } it will knows it from it self *@
    <span asp-validation-for="PhoneNumber" class="text-danger"></span> @* Message of the error *@
</div>
<div class="form-group">
    <label class="col-form-label" asp-for="HireDate"></label>
    <input asp-for="HireDate" class="form-control" />  @* asp-for="Name" ==> insted of { type="text" name="Code" id="code" } it will knows it from it self *@
</div>
<div class="form-group">
    <label class="col-form-label" asp-for="Department"></label>
    <select class="form-control" asp-for="DepartmentId" asp-items="@department">
        <option value="@null"> -- Select Department -- </option>
    </select>
</div>

<div class="form-group">
    <label class="col-form-label" asp-for="Image"></label>
    <input type="file" class="form-control-file" asp-for="Image"/>
</div>